[tool.poetry]
name = "blog-project"
version = "0.1.0"
description = ""
authors = ["Andrei <sharik.and@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.7"
Django = "3"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
